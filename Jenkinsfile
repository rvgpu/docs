pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
                sh "pwd"
                sh "ls"
                sh "make html"
            }
        }
    }
}

